# Data Deletion Assistant - Backend

FastAPI backend for automating GDPR/CCPA data deletion requests.

## Development

```bash
# Install dependencies
uv sync --all-extras

# Run tests
uv run pytest

# Run server
uv run uvicorn app.main:app --reload
```
